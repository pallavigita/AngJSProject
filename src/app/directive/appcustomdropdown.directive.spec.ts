import { AppcustomdropdownDirective } from './appcustomdropdown.directive';

describe('AppcustomdropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new AppcustomdropdownDirective();
    expect(directive).toBeTruthy();
  });
});
